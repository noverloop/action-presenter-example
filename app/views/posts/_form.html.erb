<%= form_for(presenter) do |f| %>
  <% if presenter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(presenter.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% presenter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :published %><br />
    <%= f.check_box :published %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
